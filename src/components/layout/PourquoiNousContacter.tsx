'use client';

import React from "react";

export default function PourquoiNousContacter(): React.JSX.Element {
  return (
    <p className="text-slate-500 text-xs font-semibold max-w-xl leading-relaxed pt-2 font-['Manrope',_sans-serif]">
      Une question, une suggestion d'émission ou un projet de partenariat ? <br />
      N'hésitez pas à nous laisser un message, notre équipe citoyenne vous répondra dans les plus brefs délais.
    </p>
  );
}
